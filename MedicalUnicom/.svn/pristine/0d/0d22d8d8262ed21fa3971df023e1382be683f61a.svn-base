package com.djb.ylt.user.entity;

import com.djb.ylt.framework.entity.BaseEntity;

public class GradePriceEntity extends BaseEntity {
    /**
	 * serialVersionUID:用一句话描述这个变量表示什么。
	 */
	private static final long serialVersionUID = 1L;

	private Integer gpId;

    private String positional;

    private String grade;

    private Double vaPrice;

    private Double tiPrice;

    private Double viPrice;

    private Double taPrice;

    private Double paPrice;

    private Double piPrice;

    private Integer vCount;

    private Integer tCount;
    
    private Double pvPrice;
    
    private Double ptPrice;
    
    private String  effectTime;
    
    private String gpType;
    
    private Double gpPrice;
    
   private String workType;
    
    private String earlyTime;
    
    private Double  dayTotal;
    
    private Double  nightTotal;
    
    private Double photoPrice;
    
    private Integer freeTotal;

    public Integer getGpId() {
        return gpId;
    }

    public void setGpId(Integer gpId) {
        this.gpId = gpId;
    }

    public String getPositional() {
        return positional;
    }

    public void setPositional(String positional) {
        this.positional = positional;
    }

    public String getGrade() {
        return grade;
    }

    public void setGrade(String grade) {
        this.grade = grade;
    }




    public Integer getvCount() {
        return vCount;
    }

    public void setvCount(Integer vCount) {
        this.vCount = vCount;
    }

    public Integer gettCount() {
        return tCount;
    }

    public void settCount(Integer tCount) {
        this.tCount = tCount;
    }

    @Override
    public boolean equals(Object that) {
        if (this == that) {
            return true;
        }
        if (that == null) {
            return false;
        }
        if (getClass() != that.getClass()) {
            return false;
        }
        GradePriceEntity other = (GradePriceEntity) that;
        return (this.getGpId() == null ? other.getGpId() == null : this.getGpId().equals(other.getGpId()))
            && (this.getPositional() == null ? other.getPositional() == null : this.getPositional().equals(other.getPositional()))
            && (this.getGrade() == null ? other.getGrade() == null : this.getGrade().equals(other.getGrade()))
            && (this.getVaPrice() == null ? other.getVaPrice() == null : this.getVaPrice().equals(other.getVaPrice()))
            && (this.getTiPrice() == null ? other.getTiPrice() == null : this.getTiPrice().equals(other.getTiPrice()))
            && (this.getViPrice() == null ? other.getViPrice() == null : this.getViPrice().equals(other.getViPrice()))
            && (this.getTaPrice() == null ? other.getTaPrice() == null : this.getTaPrice().equals(other.getTaPrice()))
            && (this.getPaPrice() == null ? other.getPaPrice() == null : this.getPaPrice().equals(other.getPaPrice()))
            && (this.getPiPrice() == null ? other.getPiPrice() == null : this.getPiPrice().equals(other.getPiPrice()))
            && (this.getvCount() == null ? other.getvCount() == null : this.getvCount().equals(other.getvCount()))
            && (this.gettCount() == null ? other.gettCount() == null : this.gettCount().equals(other.gettCount()));
    }

    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((getGpId() == null) ? 0 : getGpId().hashCode());
        result = prime * result + ((getPositional() == null) ? 0 : getPositional().hashCode());
        result = prime * result + ((getGrade() == null) ? 0 : getGrade().hashCode());
        result = prime * result + ((getVaPrice() == null) ? 0 : getVaPrice().hashCode());
        result = prime * result + ((getTiPrice() == null) ? 0 : getTiPrice().hashCode());
        result = prime * result + ((getViPrice() == null) ? 0 : getViPrice().hashCode());
        result = prime * result + ((getTaPrice() == null) ? 0 : getTaPrice().hashCode());
        result = prime * result + ((getPaPrice() == null) ? 0 : getPaPrice().hashCode());
        result = prime * result + ((getPiPrice() == null) ? 0 : getPiPrice().hashCode());
        result = prime * result + ((getvCount() == null) ? 0 : getvCount().hashCode());
        result = prime * result + ((gettCount() == null) ? 0 : gettCount().hashCode());
        return result;
    }


	/**
	 * 返回effectTime的值
	 * @return String effectTime的值
	 */
	public String getEffectTime() {
		return effectTime;
	}

	/**
	 * 设置effectTime的值
	 * @param  effectTime effectTime的值
	 */
	public void setEffectTime(String effectTime) {
		this.effectTime = effectTime;
	}

	/**
	 * 返回gpType的值
	 * @return String gpType的值
	 */
	public String getGpType() {
		return gpType;
	}

	/**
	 * 设置gpType的值
	 * @param  gpType gpType的值
	 */
	public void setGpType(String gpType) {
		this.gpType = gpType;
	}

	/**
	 * 返回vaPrice的值
	 * @return Double vaPrice的值
	 */
	public Double getVaPrice() {
		return vaPrice;
	}

	/**
	 * 设置vaPrice的值
	 * @param  vaPrice vaPrice的值
	 */
	public void setVaPrice(Double vaPrice) {
		this.vaPrice = vaPrice;
	}

	/**
	 * 返回tiPrice的值
	 * @return Double tiPrice的值
	 */
	public Double getTiPrice() {
		return tiPrice;
	}

	/**
	 * 设置tiPrice的值
	 * @param  tiPrice tiPrice的值
	 */
	public void setTiPrice(Double tiPrice) {
		this.tiPrice = tiPrice;
	}

	/**
	 * 返回viPrice的值
	 * @return Double viPrice的值
	 */
	public Double getViPrice() {
		return viPrice;
	}

	/**
	 * 设置viPrice的值
	 * @param  viPrice viPrice的值
	 */
	public void setViPrice(Double viPrice) {
		this.viPrice = viPrice;
	}

	/**
	 * 返回taPrice的值
	 * @return Double taPrice的值
	 */
	public Double getTaPrice() {
		return taPrice;
	}

	/**
	 * 设置taPrice的值
	 * @param  taPrice taPrice的值
	 */
	public void setTaPrice(Double taPrice) {
		this.taPrice = taPrice;
	}

	/**
	 * 返回paPrice的值
	 * @return Double paPrice的值
	 */
	public Double getPaPrice() {
		return paPrice;
	}

	/**
	 * 设置paPrice的值
	 * @param  paPrice paPrice的值
	 */
	public void setPaPrice(Double paPrice) {
		this.paPrice = paPrice;
	}

	/**
	 * 返回piPrice的值
	 * @return Double piPrice的值
	 */
	public Double getPiPrice() {
		return piPrice;
	}

	/**
	 * 设置piPrice的值
	 * @param  piPrice piPrice的值
	 */
	public void setPiPrice(Double piPrice) {
		this.piPrice = piPrice;
	}

	/**
	 * 返回pvPrice的值
	 * @return Double pvPrice的值
	 */
	public Double getPvPrice() {
		return pvPrice;
	}

	/**
	 * 设置pvPrice的值
	 * @param  pvPrice pvPrice的值
	 */
	public void setPvPrice(Double pvPrice) {
		this.pvPrice = pvPrice;
	}

	/**
	 * 返回ptPrice的值
	 * @return Double ptPrice的值
	 */
	public Double getPtPrice() {
		return ptPrice;
	}

	/**
	 * 设置ptPrice的值
	 * @param  ptPrice ptPrice的值
	 */
	public void setPtPrice(Double ptPrice) {
		this.ptPrice = ptPrice;
	}

	/**
	 * 返回gpPrice的值
	 * @return Double gpPrice的值
	 */
	public Double getGpPrice() {
		return gpPrice;
	}

	/**
	 * 设置gpPrice的值
	 * @param  gpPrice gpPrice的值
	 */
	public void setGpPrice(Double gpPrice) {
		this.gpPrice = gpPrice;
	}

	/**
	 * 返回workType的值
	 * @return String workType的值
	 */
	public String getWorkType() {
		return workType;
	}

	/**
	 * 设置workType的值
	 * @param  workType workType的值
	 */
	public void setWorkType(String workType) {
		this.workType = workType;
	}

	/**
	 * 返回earlyTime的值
	 * @return String earlyTime的值
	 */
	public String getEarlyTime() {
		return earlyTime;
	}

	/**
	 * 设置earlyTime的值
	 * @param  earlyTime earlyTime的值
	 */
	public void setEarlyTime(String earlyTime) {
		this.earlyTime = earlyTime;
	}

	/**
	 * 返回dayTotal的值
	 * @return Double dayTotal的值
	 */
	public Double getDayTotal() {
		return dayTotal;
	}

	/**
	 * 设置dayTotal的值
	 * @param  dayTotal dayTotal的值
	 */
	public void setDayTotal(Double dayTotal) {
		this.dayTotal = dayTotal;
	}

	/**
	 * 返回nightTotal的值
	 * @return Double nightTotal的值
	 */
	public Double getNightTotal() {
		return nightTotal;
	}

	/**
	 * 设置nightTotal的值
	 * @param  nightTotal nightTotal的值
	 */
	public void setNightTotal(Double nightTotal) {
		this.nightTotal = nightTotal;
	}

	/**
	 * 返回photoPrice的值
	 * @return Double photoPrice的值
	 */
	public Double getPhotoPrice() {
		return photoPrice;
	}

	/**
	 * 设置photoPrice的值
	 * @param  photoPrice photoPrice的值
	 */
	public void setPhotoPrice(Double photoPrice) {
		this.photoPrice = photoPrice;
	}

	/**
	 * 返回freeTotal的值
	 * @return Integer freeTotal的值
	 */
	public Integer getFreeTotal() {
		return freeTotal;
	}

	/**
	 * 设置freeTotal的值
	 * @param  freeTotal freeTotal的值
	 */
	public void setFreeTotal(Integer freeTotal) {
		this.freeTotal = freeTotal;
	}


    
}